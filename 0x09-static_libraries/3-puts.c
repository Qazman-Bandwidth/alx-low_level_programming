#include "main.h"

void _puts(char *str);

#include<stdio.h>
/**
 * main - The program to print with puts
 * Return: Always 0
 */

int main(void)
{
	puts("Grit makes me going no matter the obstacles");
	return (0);
}
