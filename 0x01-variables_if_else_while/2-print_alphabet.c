#include <stdio.h>

/**
 * main - A program that print alphabet in lower case
 *
 * Return: 0 (Success)
 */

int main(void)
{
	for (char i = 'a'; i <= 'z'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
