#include <stdio.h>

/**
 * main - A program that prints all single digit numbers to base 0
 *
 * Return: 0 (success)
 */

int main(void)
{
int i;
for (int i = 0; i < 10; i++)
printf("%d", i);
printf("\n");
return (0);
}
